---
pageKey: groups
---
{#- The groups selected by the artistic committee. Empty until the committee decides, when
    groups-list.njk prints the coming-soon note instead; that is why this page stays out of the
    menu while the call runs (see content/pages.yaml). -#}
<section class="wrap section">
  <h1>{{ registry.title | t(lang) }} {{ edition.year }}</h1>
</section>
<section class="wrap section">
  {% include "partials/groups-list.njk" %}
</section>
